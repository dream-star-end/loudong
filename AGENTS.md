## Cursor Cloud specific instructions

This repository is currently empty (single `test` file, no source code or project configuration). There are no dependencies to install, no services to run, and no build/lint/test commands available.

When source code is added, update this section with relevant development instructions.
